# 智能指针：从入门到入土

> SJTU CS1958-01 2026Fall 第二次大作业 前置阅读材料

> 由于笔者能力有限，不保证材料内容完全准确，仅用于辅助理解

## 一、前置知识

### 1. 指针

先来看一个 OIer 都非常熟悉的例子：

```cpp
int a;
scanf("%d", &a);
```

想必各位在初次接触到 `scanf` 的时候都会疑惑，为什么在传入参数的时候一定要加上一个 `&` 呢？这是因为 `scanf` 在读入并修改 `a` 的数值的时候，必须要知道将输入的数据存储到哪一块内存中，而 `&a` 就是变量 `a` 所在内存的起始地址（例如该地址为 `0x7ffc1234`），通过 `&`（取地址运算符）即可获得这一地址。这里的 `&a` 在 C++ 的类型系统中被记作 `int*`，即“指向 `int` 的指针”。

如果我们希望新开一个变量 `ptr`，让它也指向 `a` 所在的地址，可以这样写：

```cpp
int* ptr = &a;
```

此时 `ptr` 存储了 `&a` 这个地址值的**一份拷贝**，即 `ptr` 和 `&a` 在数值上相同，都指向 `a` 所在的内存地址。但请注意，`&a` 本身只是一个临时的地址值（纯右值，详见以下章节），而 `ptr` 是一个具名的变量，可以被取地址（`&ptr`），这是两者在语言层面的本质区别。

接下来看解引用操作。如果我们通过 `*ptr`（间接寻址运算符，即解引用）修改了这块地址上的内容，那么 `a` 的值也会相应发生变化：

```cpp
int a = 10;
int* ptr = &a;
*ptr = 20;
std::cout << a << std::endl; // 输出20
```

这是因为 `*ptr` 直接修改了地址 `0x7ffc1234` 上存储的值，而 `a` 恰好就住在这个地址上。

与之形成对比的是普通变量的赋值：

```cpp
int a = 10;
int b = a;
b = 20;
std::cout << a << std::endl; // 输出10
```

`a` 和 `b` 是两块不同的内存空间，修改 `b` 不会影响 `a`。这正是“通过指针间接修改”与“直接操作变量副本”的核心区别。

值得注意的是，指针指向的内存中存储的内容不一定是 `int`、`float` 这样的基本类型。由于程序运行中的所有数据都在内存中，指针本身也占据内存，因此可以构造**指向指针的指针**，即多级指针：

```cpp
int** ptr2 = &ptr; // ptr2 存储了 ptr 这个指针变量的地址
int*** ptr3 = &ptr2; // 以此类推
```

虽然在实际工程中，超过三级的指针已经很少见，但理解其“层层解引用”的逻辑，对后续理解复杂数据结构的嵌套访问（如二维动态数组、链表节点中的指针域等）会很有帮助。

上述例子中我们只用到了`int*`（更多级也一样），可以把它理解成一种指针类型，它只是栈上的一个变量，必须指向某块已有的内存（不指向的话，就会直接变成野指针）。而如果我们想要进行新的内存分配，就要使用`new`（C++）或是`malloc`（C）进行动态申请。`new`的过程一共分为三步：分配内存、在内存的位置上调用构造函数、返回内存的地址。两者的区别可以用如下代码区分：

```cpp
// Case 1 (invalid)
int* p1;
*p1 = 10; // p1指向随机地址，我们无权写入。

// Case 2 (valid)
int a = 10;
int* p2 = &a; // p2指向已有的内存&a

// Case 3 (valid)
int* p3 = new int; // p3指向一块未初始化的int内存
*p3 = 20; // *p3为堆上的值，可以修改
delete p3;

// Case 4 (valid)
int* p4 = new int(30); // p4指向一块初始化为30的int内存
delete p4;
```

 显然`new T`一次只能申请一块`T`类型的内存空间，对于大量的存储（例如`int a[100000]`）来说效率过于低下，且即便连续进行多次这样的申请，也无法保证内存地址的连续性，使得随机访问变得非常麻烦。故而有另外一种**数组类型指针**的内存申请的方式`new T[num]`（`num`为任意数值），可以在内存空间中一口气申请一共`num`块`T`类型的空间，还能保证这些空间的连续性。但以数组类型指针方式申请内存时一定要牢记，在释放内存时要用`delete[]`而非`delete`来释放：

```cpp
int* p5 = new int[10];
delete[] p5;
```

有趣的是，无论我们申请的内存空间有多少，在`delete`的时候都不需要告诉程序，只需要简单的`[]`就能解决。根据笔者自己的探索，这涉及到`new T[]`之后的**元数据**存储方式与`new T`的区别，`new T[]`会在某个特定的地方专门存储申请连续空间的长度。这也就解释了前文中为什么强制要求`new T`和`new T()`用`delete`，而`new T[]`必须用`delete[]`的原因。

元数据的设计和存储是一个非常有意思的课题，感兴趣的同学可以自行探索不同函数和工具的元数据存储方式。

### 2. 引用（左值引用）

引用（特指左值引用，即 `T&`，**左值**和**右值**的区分详见下文）从语法上看是变量的**别名**，但其底层本质是一个**自带自动解引用特性的指针**（汇编层面等价于 `T* const`，即指向不可变的指针）。例如：

```cpp
int a = 10;
int& b = a;
b = 20;
std::cout << a << std::endl; // 输出20
```

由这个例子可见，`b`与`a`在地址上进行了深度的绑定，在修改`b`的时候，编译器就自动完成了取地址和解引用的操作，直接对`a`所在的地址进行修改，因此`a`的值也就相应发生了变化。

引用也可以用在函数的参数传递中。例如如果我们想要自己实现`swap`函数，如果不使用引用而写成如下版本的话，将没有任何意义：

```cpp
void swap(int a, int b)
{
    int tmp = a;
    a = b;
    b = tmp;
}
```

原因在于C++的函数参数传递是拷贝传递而非引用传递，在函数体之外调用`swap(c, d)`的时候，程序并没有将`c`和`d`的地址传入`swap`函数，而是将两者的值拷贝了一份到函数体的`a`和`b`中。接下来在`swap`函数体中的操作只是对这些临时变量进行修改，却没有影响到函数体外的`c`和`d`。但如果使用引用传递，即定义函数时写作：

```cpp
void swap(int& a, int& b)
{
    ...
}
```

则可以保证调用`swap`函数时修改的就是`c`和`d`本身。

需要注意的是，C++标准对引用提出了以下三点硬性要求，在使用时一定要加以注意：

1. **必须初始化**：`int &ref;` 是编译错误。不存在“空引用”，因此省去了指针判空（`if (ptr != nullptr)`）的麻烦。

2. **不可重新绑定**：引用一旦绑定到 `a`，其终身指向 `a`。执行 `ref = b;` 并不会让 `ref` 改指向 `b`，而是把 `b` 的值**赋值给** `a`。这也意味着引用的指针是不存在的（即`int&*`），因为引用不是对象。

3. **不延长生命周期**：返回局部变量的引用依然是悬挂引用（未定义行为），引用本身不管理内存所有权。

事实上，引用与指针在**汇编**层面**没有区别**，是指针的一个语法糖。你可以理解为，引用只是C++编译器对一个常量指针作了一些封装，使得其可以自动完成取地址、解引用的功能，上文中引用区别于指针的特性都是编译器约束完成的，一旦编译成汇编就和指针一样。由于引用只是指针包装了下，所以也存在风险，比如如下代码：

```cpp
int* a = new int;
int& b = *a;
delete a;
b = 12;
// 对已经释放的内存解引用
```

所以由编译器保证初始化的引用相较于指针而言使用起来较为方便(如不用检查空指针等)，所以建议尽量用引用代替指针。

### 3. 左值与右值

前文提到，引用一般特指为左值引用。有左值就肯定有右值，那么这两者之间又有什么区别呢？

从定义上很好区分这两者：

- **左值**：拥有持久的内存地址，可以取地址（`&`），生命周期由**变量作用域**决定。

- **右值**：没有持久内存地址的临时值，要么是字面量（如 `5`），要么是计算过程中的中间结果（如 `a + b`），要么是函数返回的临时对象，当前语句**结束后即被销毁**。

换句话说，就是“能取地址的就是左值，不能取地址的就是右值”。

我们来看一个例子：

```cpp
int a = 5;
int* p1 = &a; // valid, because a is lvalue
int* p2 = &5; // invalid, because 5 is rvalue
```

初学者可能还会以“能否被修改”为标准区分左右值，这诚然是满足大部分情况的，因为能否取地址**一般情况下**确实与能否修改有强关联，但在以下例子中，该判据失效：

```cpp
const int a = 5;
```

虽然`a`作为常量，只能在初始化时定义而不能被赋值，但它需要开辟一块内存空间来存储`a`的值，因此`&a`操作是合法的，它拥有持久的内存地址，因此是**不可修改的左值**。

注意到这一特性之后，我们自然便会联想到，**常量左值引用**（`const T&`）也是不可修改的，那么它是不是也能绑定右值呢？的确如此。在早期C++版本中，为了支持对临时变量的绑定，专门引入了一个特性：常量左值引用不仅可以绑定左值，还能绑定不可修改的左值和右值，代码如下所示：

```cpp
#include <iostream>
void printRef(const int& y) // 常量引用
{ 
    std::cout << y << '\n';
}

int main()
{
    int x = 5;
    const int z = 10;

    printRef(x); // 可修改左值
    printRef(z); // 不可修改左值
    printRef(42); // 右值字面量，绑定到临时对象
}
```

在执行`printRef(42)`时，程序会生成一个临时`int`，生命周期延长到`printRef`执行结束。（当然这个知识点和本次大作业没啥关系）

在绝大多数情况下，绑定右值都是毫无意义的，因为它们只是一个临时值，连自己的内存地址都没有，引用它们显然徒劳无功。然而，在C++11中却专门引入了**右值引用**（`T&&`），专门用于捕获这些即将销毁的对象，这是为什么呢？

### 4. 右值引用

引入右值引用的原因可以从下面这个例子中直观体现——

众所周知，`std::vector`是一个非常方便的STL容器，它的长度可以灵活调整，我们随时能够通过`push_back`操作往容器中添加新的元素，同时使它的长度发生改变。

但是，前面我们提到过，程序中的所有数据都会存储在内存当中（当然，学习完左值和右值之后，我们应该明白这些数据不会包括右值）。从这一点出发，我们不妨考虑一下`std::vector`的实现方式：它里面的数据应当如何存储呢？

一种直接的想法是，每当我`push_back`一个新的元素时，就为它申请一块新的内存空间用于存储，同时在前一块内存空间中记录它下一块内存空间的地址信息。但显而易见的是我们此时设计出了一个链表，并不能满足`std::vector`的随机访问的需求。

要想随机访问必须保证数据存储的连续性，但根据前文所述，即便是使用`new T[]`，也只能申请连续`num`块内存空间，但`num`作为预先就确定下来的常量，显然不能保证一定能满足`std::vector`无限延伸的需求，且如果`num`的值过大而需要用到的空间又不大的话，又会产生空间上的浪费。

由此自然产生的想法就是，我们每次一开始申请的时候，都只申请一小段连续内存。当连续内存不够用之后，再申请一段更长的连续内存（一种常见的做法是原内存长度的2倍），将原来的数据全部**搬运**到新的连续内存当中，再把老的连续内存全部释放掉。这样做既保证了访问的随机性，也保证了能随时往里面添加新的元素。

看上去一切都非常完美，但有一个重要的问题却被我们忽略了：应该如何高效**搬运**数据？

最常用的方式肯定是拷贝构造，它也的确能够完成我们需要的搬运工作。

```cpp
vector(const vector& other) // 拷贝构造函数
{
    size_ = other.size_;
    data_ = new int[size_];
    for (int i = 0; i < size_; ++i)
    {
        data_[i] = other.data_[i];
    }
}
```

然而拷贝构造是一个非常缓慢的过程，而且原内存中的数据在本次拷贝之后将不再有任何作用，被立即释放。专门拷贝一份不需要的数据显然是毫无必要的，正如我们在**同一个电脑磁盘内**搬运某个大文件时，显然不需要先将这个文件拷贝到新的地址，再将原地址上的该文件删除，只需要进行剪切操作，也就是目录索引位置的转移即可，这种方式显然更加迅速。

揆诸`std::vector`的例子亦是如此，我们希望设计一种全新的搬运方式，使得数据的搬运不再是繁琐的拷贝+删除，而是像剪切操作那般只发生所有权的转移。这就是**移动构造**。

然而还有最最最最最后一个问题：构造函数的格式是固定的，区分不同种类的构造函数只能通过传入参数的数量和种类进行判断，因此我们设计的移动构造函数的参数必然要在数量或种类上与拷贝构造函数区分开。如果像这样区分：

```cpp
vector(vector& other, bool move) // 移动构造函数
{
    data_ = other.data_;
    size_ = other.size_;
    other.data_ = nullptr;
    other.size_ = 0;
}
```

这种设计确实可以工作，但有两个严重的问题无法规避：

- 需要一个额外的`move`参数来判定是拷贝构造还是移动构造。倘若删去这一参数，那么它将无法和拷贝构造区分开，因为传入的左值`vector`既满足拷贝构造函数的要求，又满足移动构造的函数的要求，那么移动构造就可能变成抢劫式拷贝构造，使得原本应当被拷贝的数据却错误地被删除了。

- 无法传入右值，例如`vector`的默认构造函数作为右值，就无法被移动构造函数的参数识别。倘若为`other`加上`const`修饰，前文虽然提及常量左值引用`const T&`可以绑定右值，但移动构造中的`other`需要被修改，与`const`的修饰要求矛盾。

有一个很有意思的细节：移动后原数据将立即失效，那么原来的数据就可以看作是某种临时变量，与右值的性质非常类似。那么，我们是不是就可以在传入参数的时候，直接将它视为一种右值呢？

基于此，C++11引入了一种叫做**右值引用**（`T&&`）的功能，有别于常量左值引用能绑定左值，也能绑定右值，右值引用限定了绑定的种类只能是右值，避免与拷贝构造的左值绑定区分开。同时常量左值引用即便绑定了右值，“常量”的属性也规定了我们不能对引用进行任何修改，但右值引用就放开了这一限制，也与我们移动构造的目标不谋而合。

由此我们设计出了标准的移动构造函数：

```cpp
vector(vector&& other) noexcept // noexcept确保对象在移动过程中不会抛出异常
    : data_(other.data_)
    , size_(other.size_) // 使用初始化列表进行初始化，当然也可以使用原来的方式
{
    other.data_ = nullptr;
    other.size_ = 0;
}
```

此处`noexcept`关键字的作用请读者自行思考。

但是，右值引用虽然帮助我们区分开了拷贝构造和移动构造，但要求传入的参数必须是右值，如果要传入左值应该怎么做呢？解决这个问题的方法也非常简单，因为移动构造传入的参数马上就会失效，那么我们就算直接把传入的左值强行转变为右值，也不会对程序运行产生任何影响，这就是`std::move`函数的工作。这是它的源代码，它只是利用`static_cast`进行了一次强制类型转换：

```cpp
template<typename _Tp>
constexpr typename std::remove_reference<_Tp>::type&&
move(_Tp&& __t) noexcept
{
    return static_cast<typename std::remove_reference<_Tp>::type&&>(__t);
}
```

在移动构造的时候，我们只需要对传入的左值参数进行`std::move`操作变成右值即可。

细心的同学可能会发现一个问题：前文说常量左值引用也能识别右值，而拷贝构造函数传入的正是常量左值引用。那么程序面对一个右值参数传入，不还是可能会发生选择矛盾吗？这就涉及到重载决议的“精准度”法则——**编译器总是优先选择最匹配的版本**。传入的右值和右值引用完全匹配，但与常量左值引用需要一个隐式的转换，因此编译器会选择用右值引用的移动构造函数。

另外，在泛型编程当中，常常使用类模板进行适应性更强的编程，即用`template<typename T>`代表各种可能的参数类型，在使用的过程中再对需要用到的类型进行类型推导和实例化。但右值引用的引入带来了一个新的问题，那就是倘若我们需要同时实现一个函数的左值和右值版本，就需要写两份模板代码：假如有如下代码：

```cpp
template<typename T>
void foo(T&& arg)
{
    T&& arg2 = arg;
    ...
}
```

前文提到过引用不是对象，所以引用的引用从C++语法的角度来看显然是不存在的，此时根据模板实例化的规范，编译器应当报错。在这一规范下，倘若我们需要同时实现左值和右值版本的`foo`函数，似乎必须要写两份模板，即

```cpp
template<typename T>
void foo(T& arg)
{
    T& arg2 = arg;
    ...
}

template<typename T>
void foo(T&& arg)
{
    T&& arg2 = arg;
    ...
}
```

但这种实现又太过于麻烦，且两份代码重合度过高，不符合泛型编程“一份代码解决多种问题”的思想。因此C++为它强制规定了一个[推导规则](https://en.cppreference.com/cpp/language/template_argument_deduction)：

> If **P** is an rvalue reference to a cv-unqualified template parameter (so-called forwarding references), and the corresponding function call argument is an lvalue, the type lvalue reference to **A** is used in place of **A** for deduction (Note: this is the basis for the action of std::forward. Note: in class template argument deduction, template parameter of a class template is never a forwarding reference (since C++17))

同时还为这条推导规则专门设计出了一套[引用折叠规则](https://en.cppreference.com/cpp/language/reference)：

```cpp
T& & = T&
T& && = T&
T&& & = T&
T&& && = T&&
```

注意：这套规则只在“模板实例化”或“类型别名（`typedef`/`using`）”的上下文中才会生效，如果我们自己在代码中直接写出`int& &&`，那么编译器就会将其理解为引用的引用，被判定为非法而报错。

在这套引用折叠规则下，`T&&`变成了一种**万能引用**。假如我们传入的`arg`是个右值（如`int&&`），那么根据规则，类型推导会认为`T = int`，下一句中得到的`arg2`也是右值引用；但假如`arg`是个左值（如`int&`），类型推导便会认为`T = int&`，代入下一句当中便会出现`int& &&`，根据引用折叠规则，它就是`int& &&`被正确解析为了我们需要的`int&`，此时`T&&`版本的`foo`函数模板既能支持左值传递，又能支持右值传递，完美契合泛型编程的思想。同时，万能引用也为另一个非常实用的工具“完美转发”提供了成立的条件。

### 5. 完美转发

我们同样通过一个例子来看为什么需要完美转发——

假如我们要设计一个包装函数`wrapper`，从函数外接收一个引用传递的参数`arg`到函数内，在包装函数中又会以`arg`为参数调用一个内部函数`inner`，`inner`函数已经设计好两种版本，既可以接收左值，也可以接收右值。我们需要将`arg`的左右值属性通过包装函数原封不动传递给内部函数。

根据前面的“万能引用”思想，我们可以轻易写出如下代码：

```cpp
template<typename T>
void wrapper(T&& arg)
{
    inner(arg);
}
```

这种写法看似天衣无缝，但有一个致命的缺陷：无论`arg`被解析为左值引用还是右值引用，它都是一个具名变量的引用，是一定需要有一个内存地址来储存的，所以`arg`在函数体内始终是一个**左值**！所以无论`wrapper`接收到的参数是左值还是右值，等到传递到函数内部时都一律变成了左值。这种“值类别退化”并不是我们所期待的情形。

尴尬的是，这种情况并不能武断地使用`std::move`来解决，因为`std::move`会不分青红皂白地将一切左值转变为右值，导致完美引用传入左值的情形出现新的问题。

当然，这也不是没有不用到完美转发的解决方法。我们可以写两个版本的模板函数：

```cpp
template<typename T>
void wrapper(T& arg) // 左值匹配这个模板
{
    inner(arg);
}

template<typename T>
void wrapper(T&& arg) // 右值匹配这个模板，虽然左值也符合条件，但会先和上面的匹配
{
    inner(std::move(arg));
}
```

这样确实不会有问题，但它严重违背了前面提到的泛型编程的思想。同时，倘若需要调用`inner`函数的参数有很多（$N$个）的话，我们就需要实现$2^N$种不同的函数模板，在工程上显然是不现实的。

针对这一问题，C++引入了完美转发`std::forward`，它通过参数匹配识别去除引用之后元素的种类，相应地传出左值或右值并返回，使得传入的参数原封不动（保持左右值属性）地被转发给另一个函数。这是它的源代码：

```cpp
template<typename T>
T&& forward(typename std::remove_reference<T>::type& t) noexcept
{
    return static_cast<T&&>(t);
}

template<typename T>
T&& forward(typename std::remove_reference<T>::type&& t) noexcept
{
    return static_cast<T&&>(t);
}
```

它的使用方法如下：

```cpp
template<typename T>
void wrapper(T&& arg) // 万能引用
{
    inner(std::forward<T>(arg)); // 完美转发
}
```

万能引用和完美转发虽然在工程上是两个截然不同的设计，但在使用时却紧密相连：如果没有万能引用作为基石，完美转发的设计将变得毫无意义；而如果没有完美转发这样优秀的应用场景，万能引用就变成了一个只会使C++使用手册变得更厚的无关痛痒的小功能。

### 6. 模板参数包与包展开

泛型编程中，对于某一个函数而言，有时连传入参数的数量和类型都无法确定（类型甚至不一定是单一一种的`T`），只知道会有这样的一堆参数被加入。对此，C++11引入了可变参数模板`...`，样例如下：

```cpp
template<typename... Args>
void func(Args... args);
```

其中：

- `typename... Args` 声明了一个**模板类型参数包**，表示可以接受 0 个或多个类型，例如`int`、`double`、`string`的任意组合。

- `Args... args` 声明了一个**函数参数包**，表示可以接受 0 个或多个函数实参。

这样以后，在真正实例化的时候，这个`func`函数就能接受任意类型的任意数量个参数传入。熟悉python的人可以类比python中的元组`tuple`来理解它的功能。

而当`...`出现在某个模式的右侧时，就表示将该模式作用于参数包中的每一个元素，生成逗号分隔的展开列表。例如下面的`print`函数：

```cpp
template<typename... Args>
void func(Args... args)
{
    print(args...);
}
```

假如传入的`args`为`{1, 2.5, "hi"}`，那么`args...` 展开后为 `1, 2.5, "hi"`，这就是真正传入`print`函数的参数。

**注意**：模板参数包与包展开都可以与前文所提到的完美转发相配合，实现对任意类型、任意数量的参数的精准传递。

## 二、智能指针的作用

### 1. 普通指针的缺陷

普通指针尽管已经非常好用，但它在使用上依然有许多缺陷。一方面是在指针本身，例如尝试解引用一个野指针或悬空指针：

```cpp
int* p1;
int* p2 = new int(5);
delete p2;
std::cout << *p1 << std::endl; // 解引用野指针
std::cout << *p2 << std::endl; // 解引用悬空指针
```

这种错误很好避免，只需要在解引用前加上指针判空（`if (ptr != nullptr)`）就能避免对野指针的解引用（当然我们一般尽量避免直接定义一个野指针，对所有指针都进行初始化，指针判空一般用于使用完以后的指针），同时在释放内存的同时立即将指向该内存的指针置空就能避免对悬空指针的解引用。

但普通指针在内存管理上存在更大的缺陷。

第一，前文提到了，对于普通指针而言，使用`new`分配的内存必须用`delete`进行显式释放，否则会造成**内存泄漏**（Memory Leak），例如：

```cpp
int* ptr = new int;
ptr = nullptr;
// Memory Leak
```

在这个例子中，`new int`分配了一块新的内存，交给`ptr`管理（仅由它指向这块内存）。但是后来我们对`ptr`指针进行了修改（让它指向了`nullptr`或是其它地方），导致这块内存不再被任何指针指向，程序运行结束后也就无法将这块内存进行释放（计算机根本不知道这块内存的存在），造成内存泄漏。

任老师上课就分享过这样的例子：有一个交通摄像头在工作了一周之后突然无法使用了，检查之后才发现是一处`new`的内存没有`delete`，导致摄像头每次工作时都会分配出一块无法被释放的内存，最终导致数据占满了整个内存使得程序根本无法工作。由此可以一窥内存泄漏的风险。

第二，一块内存还有可能会被**双重释放**（Double Free），造成段错误或内存破坏：

```cpp
int* ptr = new int;
delete ptr;
delete ptr;
// Double Free
```

或许这些错误看上去都是可以通过极致的细心来规避的，但如果程序在`new`和`delete`之间抛出异常而终止了呢？程序在退出的时候，普通指针不会自动调用`delete`来释放内存，依然会造成Memory Leak，这是不可避免的。

因此，C++11引入了智能指针。智能指针依据[RAII](https://en.cppreference.com/cpp/language/raii)思想，对普通指针加上了一层包装，用有生命周期的对象来自动管理指针的内存，即在对象创建时自动申请内存，在对象被析构时自动释放内存，既能避免管理者手动操作时`new`以后忘记`delete`的情况，也能避免因程序异常导致内存未被释放的情况（因为在异常退出后，管理指针的智能指针对象被析构，连带着内存也被立即释放）。

*其实严格意义上讲，早在C++98当中就已经有了智能指针的雏形`auto_ptr`，但由于它的拷贝构造和赋值操作会转移所有权，源对象会被置空，既不符合传统复制语义，又不能放入各种标准容器，同时还不支持数组指针，因此在C++11当中被标记为已弃用，并在C++17中被正式移除。*

本次大作业，我们需要实现三种智能指针，分别是`unique_ptr`、`shared_ptr`、`weak_ptr`，它们各有特点，下面一一介绍。

### 2. unique_ptr

显然，`unique_ptr`具有uniqueness，即它拥有它所指向对象的唯一所有权。这是最基础的一种智能指针，通过将`unique_ptr`的生命周期和它绑定的对象内存的生命周期进行强绑定，实现对内存的自动管理。

在任何时候，`unique_ptr`都不能接受**拷贝构造**，只允许通过**移动构造**来转移对象的所有权。因此，在`unique_ptr`的实现中，我们必须显式删除拷贝构造和拷贝赋值方法，同时设计出移动构造和移动赋值的方法，还要始终牢记当对象（通过`operator=`或`reset`等方式）被其它指针接管时，原有对象的`unique_ptr`必须立即被删除。

### 3. shared_ptr

显然，一个对象只能被至多一个指针管理在实际工程应用中过于严苛，也使得引用传递等基本功能的实现变得更为复杂（需要先从`A`将对象所有权转移给`B`，等`B`用完以后再还给`A`）。可是`unique_ptr`的设计思路是“一损俱损”，指针销毁直接会导致内存释放，这强制要求了指针和对象之间必须建立双射关系。所以，这种思路不能直接应用到多个指针管理内存空间之上。

使用多个普通指针管理同一个内存空间的时候，如果使用不当，就很容易出现悬空指针或双重释放的情形，例如：

```cpp
int* p1 = new int(42);
int* p2 = p1;
delete p2;
std::cout << *p1 << std::endl; // 解引用悬空指针
```

```cpp
int* p1 = new int(42);
int* p2 = p1;
delete p1;
delete p2; // 双重释放同一块空间
```

如何使用智能指针的思路来解决这一情况呢？一个自然的想法就是使用一个内置计数器，实时计算每个对象被多少个智能指针同时管理。当一个新的共享智能指针被创建时，将计数器的值置为1（只有自己在管理），随后在拷贝和析构时进行相应的增减。当计数器清零时，自动释放它管理对象的内存空间。这种计数方式称为**强引用计数**`shared_cnt`，这种智能指针的设计称为`shared_ptr`。

### 4. weak_ptr

`shared_ptr`的设计看起来很美好，但学过oi的同学想必能一眼看出它的潜在隐患。我们可以用算法竞赛中的拓扑排序进行类比（没学过也不要紧，大一下学期的算法课上你们就会接触到这个算法了）。`shared_ptr`释放对象内存的时机是`shared_cnt`归零的那一刻，其实就像是拓扑排序算法中将每一时刻入度归零的所有点放入拓扑序中，两者的操作是很相似的。

但众所周知，拓扑排序只能在DAG上成立，倘若有向图中成环，那么环上所有点的入度都不可能减到0，也就意味着环上的所有点都无法被放入拓扑序中，拓扑排序就失效了。

揆诸`shared_ptr`亦是如此。倘若我们用`shared_ptr`实现一个双向链表或有环图，那么在析构的过程中，也会发生如有环图拓扑排序这样的情况，导致环上指针管理的内存无法释放，同样会造成内存泄漏。这种风险就是**循环引用**。我们以一个双向链表的实例为例：

```cpp
struct Node
{
    int data;
    std::shared_ptr<Node> next;
    std::shared_ptr<Node> prev;
};

int main()
{
    std::shared_ptr<Node> node1 = std::make_shared<Node>();
    std::shared_ptr<Node> node2 = std::make_shared<Node>();
    node1->next = node2;
    node2->prev = node1;
}
```

在这一过程后，`node1`和`node2`的`shared_cnt`值都是2（例如`node1`指向的内存分别被`main`中的`node1`和`node2->prev`持有）。但是，在程序结束时，`node1`和`node2`（智能指针变量）的析构只会触发两个`shared_cnt`的值都变成1，无法正常触发`Node`结构体的析构函数，也就无法让它的内存释放，导致内存泄漏。

问题的原因在于两个智能指针的相互管理，但从这个实例中我们可以发现，在使用中相互**管理**是不必要的，只需要指针`A`管理指针`B`就能保证链的完整性，而指针`B`要想回到指针`A`时，无需通过管理的途径，只需要能够**观察**到`A`的位置即可。这就是`weak_ptr`的实现思路。

通俗来讲，`weak_ptr`就是`shared_ptr`的一个弱化版本或是补丁。它持有一个非拥有（“弱”）引用，指向由`shared_ptr`管理的对象，起到观察者的作用，一方面观察有多少`shared_ptr`指向被管理的对象，一方面管理**控制块**的生命周期（而不管理对象本身的生命周期，即不会使得`shared_cnt`发生变化）。对`weak_ptr`的引用计数称为**弱引用计数**`weak_cnt`，它与`shared_cnt`被同时维护，且两者的计数方式是**相互独立**的。

>  如果你看过去年的阅读材料文档，会发现有这样一句话：当`shared_cnt`减为0时，`weak_cnt`-1。但这条规则实际上是错误的。`weak_cnt`和`shared_cnt`之间不会有任何相互影响，因为它们管理的东西是完全不同的。

为了同时维护两个数值，并实现强引用计数对对象的管理和弱引用计数对控制块的管理，往往将两个计数器和被管理的对象用一个控制块（Control Block）进行统一维护。

由于`weak_ptr`本身不能创建新的对象（前文已经提到，它指向由`shared_ptr`管理的对象），所以Control Block的创建只能由`shared_ptr`创建。因此，在一个Control Block被新建时，两个计数器的初始化值一定是`shared_cnt = 1`（代表创建它的`shared_ptr`），`weak_cnt = 0`（当前没有任何观测者）。

Control Block被析构的时间节点为`shared_cnt == 0 && weak_cnt == 0`，代表控制块中被管理的对象内存已经被释放，同时没有任何`weak_ptr`在观测这块资源的使用情况。当仅有`shared_cnt`归零时，尽管对象已经被`delete`了，但仍可能有`weak_ptr`在观测它，程序可能需要通过这些`weak_ptr`（的`expired`方法）来确认这块内存是否被释放，因此Control Block不能被立即析构。

当理解了这一点之后，我们就能明白对Control Block的管理和对指针所指对象的管理是几乎完全不相干的两件事，因此两个计数器之间也不会互相影响。

`weak_ptr`的一个重要作用就是破除我们上文提到的循环引用：

```cpp
struct Node
{
    int data;
    std::shared_ptr<Node> next;
    std::weak_ptr<Node> prev; // 将其中一个管理性质的强引用变成观察性质的弱引用
};

int main()
{
    std::shared_ptr<Node> node1 = std::make_shared<Node>();
    std::shared_ptr<Node> node2 = std::make_shared<Node>();
    node1->next = node2;
    node2->prev = node1;
}
```

详细步骤如下：

- 在这一过程后，`node1`的`shared_cnt = 1`，`weak_cnt = 1`，`node2`的`shared_cnt = 2`，`weak_cnt = 0`；

- 根据栈的规则，先触发`node2`（智能指针对象）的析构函数，使得它的`shared_cnt`减一，变成1；

- 再触发`node1`（智能指针对象）的析构函数，`node1`的`shared_cnt`减一；

- `node1`的`shared_cnt`此时归零，触发`Node`（结构体）的析构函数，`node1`所管理的`data`内存被释放，同时`node1->next`指向的`node2`的`shared_cnt`减一；

- `node2`的`shared_cnt`此时归零，触发`Node`（结构体）的析构函数，`node2`所管理的`data`内存被释放，同时`node2->prev`指向的`node1`的`weak_cnt`减一。`node2`的`shared_cnt`和`weak_cnt`都是0也触发了`node2`的Control Block析构；

- `node1`的`shared_cnt`和`weak_cnt`都归零，触发`node1`的Control Block析构。

不难发现，`weak_cnt`通过观察的方式实现了功能需要的相互链接，但又同时破除了`shared_cnt`成环导致的引用循环。

因为 `weak_ptr` 不控制生命周期，它不能直接解引用，但它可以通过`lock()`方法临时提升为 `shared_ptr`（会使得`shared_cnt`计数加一）。

这三种智能指针的几种需要实现的详细方法及其规则详见项目正式的[README](./README.md)。
